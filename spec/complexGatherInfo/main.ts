import * as b from "./bobril.fake";
import * as assets from "./assets/assets";

const hereIcon = "here";

let $a=b.sprite("image");
let $b=b.sprite(hereIcon);
let $c=b.sprite(assets.addIcon);
